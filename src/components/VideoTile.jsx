import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const VideoImg = styled(Box) (() => ({
    display: 'block',
    width: 'full',
    height: 'full',
    objectFit: 'cover',
    position: 'relative'
    }
)) 
